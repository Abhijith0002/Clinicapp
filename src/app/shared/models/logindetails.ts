export class Logindetails {
    id: number=0;
    user: number=0;
    login_time: string='';
    logout_time: string='';
    staff_name: string='';
    staff_role: string='';

    // constructor() {
    //   // Set logout_time to the current date and time when creating an instance
    //   // This will only happen if logout_time is initially null
    //   if (this.logout_time === null) {
    //     this.logout_time = new Date().toISOString();
    //   }
    // }
}